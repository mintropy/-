stages:
  - build 
  - deploy

build:
  stage: build
  script:
    - docker-compose build
  tags:
    - dev

deploy:
  stage: deploy
  script:
    # - docker-compose run backend python manage.py migrate
    - docker-compose up -d
  tags:
    - dev
