stages:
  - build 
  - deploy

build:
  stage: build
  script:
    - docker-compose build
  tags:
    - dev

deploy:
  stage: deploy
  script:
    - docker-compose up -d
    - docker-compose run backend python manage.py migrate
  tags:
    - dev
